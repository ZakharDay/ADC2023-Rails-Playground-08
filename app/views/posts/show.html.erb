<p style="color: green"><%= notice %></p>

<%= render @post %>

<div>
  <%= link_to "Edit this post", edit_post_path(@post) %> |
  <%= link_to "Back to posts", posts_path %>

  <%= button_to "Destroy this post", post_path(@post), method: :delete %>
</div>

<h2>PolyComments</h2>
<%# if can? :create, PolyComment %>
  <%= render "poly_comments/form", commentable: @post, poly_comment: PolyComment.new %>
<%# end %>

<%= render @post.poly_comments %>