<%= form_with(model: [@gallery, gallery_image]) do |form| %>
  <% if gallery_image.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(gallery_image.errors.count, "error") %> prohibited this gallery_image from being saved:</h2>

      <ul>
        <% gallery_image.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :gallery_id, style: "display: block" %>
    <%= form.number_field :gallery_id %>
  </div>

  <div>
    <%= form.label :image, style: "display: block" %>
    <%= form.file_field :image %>
  </div>

  <div>
    <%= form.label :position, style: "display: block" %>
    <%= form.number_field :position %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
